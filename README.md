# S4 grating
Starting script for a grating in S4, launched and plotted with python

* Run the grating simulation with `python launch.py`
* The simulation outputs `spectrum.txt`
* Plot `spectrum.txt` with `plot_spectrum.py`
